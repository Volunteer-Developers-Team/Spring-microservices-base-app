# App Configs
# --------------------------------------------------------------
spring:
  profiles:
    active: development
  application:
    name: sample-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888

server:
  port: 9091